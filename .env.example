# Environment configuration example
# Copy this file to .env and fill in your values if needed
# This project doesn't require secrets by default, but environment variables can be used for configuration

# Example: API keys or credentials (if needed in the future)
# API_KEY=your_value_here
# DATABASE_URL=your_value_here

# Example: Runtime configuration
# LOG_LEVEL=INFO
# DEBUG=False
